   // Secure Origin validation: https:// or localhost are accepted in order to access the computer webcam
   // index.html has to be through any kind of secured server or local server running
   > npm install 
   // or download npm from npm.org o download node.js
   > npm start 
   //this will run the included package.json>script to start the local server on the browser using a port such as 3000

